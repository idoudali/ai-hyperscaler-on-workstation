<domain type='kvm'>
  <name>{{ vm_name }}</name>
  <uuid>{{ vm_uuid | default('') }}</uuid>
  <memory unit='GiB'>{{ memory_gb }}</memory>
  <currentMemory unit='GiB'>{{ memory_gb }}</currentMemory>
  <vcpu placement='static'>{{ cpu_cores }}</vcpu>

  <!-- Memory backend configuration matching working script -->
  <memoryBacking>
    <source type='memfd'/>
    <access mode='shared'/>
    {% if hardware.get('use_hugepages', false) %}
    <hugepages>
      {% if hardware.get('hugepage_size_gb') %}
      <page size='{{ hardware.get('hugepage_size_gb') }}' unit='GiB'/>
      {% endif %}
    </hugepages>
    {% endif %}
  </memoryBacking>


  <os>
    <type arch='x86_64' machine='q35'>hvm</type>
    <boot dev='hd'/>
  </os>

  <features>
    <acpi/>
    <apic/>
    <vmport state='off'/>
    <hyperv>
      <relaxed state='on'/>
      <vapic state='on'/>
      <spinlocks state='on' retries='8191'/>
      <vendor_id state='on' value='kvm hyperv'/>
    </hyperv>
    <kvm>
      <hidden state='on'/>
    </kvm>
    <ioapic driver='qemu'/>
    <smm state='on'/>
  </features>

  <!-- CPU configuration matching working script -->
  <cpu mode='host-passthrough'>
    <topology sockets='1' cores='{{ cpu_cores }}' threads='1'/>
    <feature policy='disable' name='hypervisor'/>
    <feature policy='require' name='topoext'/>
    <feature policy='disable' name='la57'/>
  </cpu>

  <clock offset='utc'>
    <timer name='rtc' tickpolicy='catchup'/>
    <timer name='pit' tickpolicy='delay'/>
    <timer name='hpet' present='no'/>
  </clock>

  <on_poweroff>destroy</on_poweroff>
  <on_reboot>restart</on_reboot>
  <on_crash>restart</on_crash>

  <!-- QEMU arguments to match working script configuration -->
  <qemu:commandline xmlns:qemu='http://libvirt.org/schemas/domain/qemu/1.0'>
    <qemu:arg value='-cpu'/>
    <qemu:arg value='host,host-phys-bits=on,host-phys-bits-limit=39'/>
  </qemu:commandline>

  <devices>
    <emulator>/usr/bin/qemu-system-x86_64</emulator>

    <!-- Primary disk (with snapshot protection like working script) -->
    <disk type='file' device='disk'>
      <driver name='qemu' type='qcow2' snapshot='external'/>
      <source file='{{ disk_path }}'/>
      <target dev='vda' bus='virtio'/>
    </disk>

    <!-- Network interface -->
    <!-- Note: Working script uses user networking with port forwarding -->
    <!-- For cluster setup, we use bridged networking instead -->
    <interface type='network'>
      <source network='{{ cluster_name }}-network'/>
      <model type='virtio'/>
      {% if mac_address %}
      <mac address='{{ mac_address }}'/>
      {% endif %}
    </interface>

    <!-- Basic controllers -->
    <controller type='usb' index='0' model='qemu-xhci'/>
    <controller type='pci' index='0' model='pcie-root'/>

    <!-- GPU Passthrough (simplified to match working script) -->
    {% if pcie_passthrough and pcie_passthrough.get('enabled') and pcie_passthrough.get('devices') %}
    {% for device in pcie_passthrough['devices'] %}
    <hostdev mode='subsystem' type='pci' managed='yes'>
      <driver name='vfio'/>
      <source>
        <address domain='0x{{ device['pci_address'].split(":")[0] }}'
                 bus='0x{{ device['pci_address'].split(":")[1] }}'
                 slot='0x{{ device['pci_address'].split(":")[2].split(".")[0] }}'
                 function='0x{{ device['pci_address'].split(":")[2].split(".")[1] }}'/>
      </source>
      {% if device.get('device_type') == 'gpu' %}
      <address type='pci' multifunction='on'/>
      {% endif %}
    </hostdev>
    {% endfor %}
    {% endif %}

    <!-- Console access (simplified, matching working script's nographic approach) -->
    <serial type='file'>
      <source path='{{ trace_folder }}/{{ vm_name }}-console.log'/>
      <target type='isa-serial' port='0'/>
    </serial>
    <console type='file'>
      <source path='{{ trace_folder }}/{{ vm_name }}-console.log'/>
      <target type='serial' port='0'/>
    </console>

    <!-- No graphics when using GPU passthrough (matches working script -nographic) -->

    <!-- Essential devices -->
    <input type='keyboard' bus='ps2'/>
    <input type='mouse' bus='ps2'/>
    <memballoon model='none'/>
    <rng model='virtio'>
      <backend model='random'>/dev/urandom</backend>
    </rng>
  </devices>
</domain>
