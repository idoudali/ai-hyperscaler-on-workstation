# Default DNS configuration for cluster nodes
dns_nameservers:
  - 8.8.8.8
  - 1.1.1.1
dns_search: []

# auto | true | false
# auto: detect systemd-resolved and configure it when active
dns_use_systemd_resolved: auto
