# Ansible Core and Dependencies
# Core Ansible installation
# Pin to Ansible 10.x for Kubespray 2.29.0 compatibility (requires Ansible core 2.17.3 to < 2.18.0)
# Ansible version mapping: Ansible 10.x = Ansible core 2.17.x
ansible>=10.0.0,<11.0.0
ansible-lint>=8.0.0


# Python dependencies for Ansible
rich>=13.0.0
tabulate>=0.9.0
pyyaml>=6.0
jinja2>=3.1.0
cryptography>=41.0.0
paramiko>=3.0.0
netaddr>=0.10.0
jmespath>=1.0.0  # Required for json_query filter used by Kubespray
